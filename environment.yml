# create:
# conda env create --file environment.yml
# update:
# conda env update --file environment.yml --prune
name: pyknos

channels:
  - conda-forge
  - pytorch

dependencies:
  - autoflake
  - black
  - cudatoolkit
  - flake8
  - isort
  - jupyter
  - matplotlib
  - nbstripout
  - numpy
  - pip
  - pip:
  - "python >= 3.6.0"
  - tensorboard
  - tqdm
 
